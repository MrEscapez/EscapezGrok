# EscapezCraft Staff Panel — frontend env example
# Alleen publieke URL; nooit secrets / RCON in de client.
#
# Dev (aanbevolen): relatief pad via Vite proxy → zelfde origin, cookies makkelijk
VITE_API_URL=/api/v1
#
# Alternatief zonder proxy (CORS credentials op backend):
# VITE_API_URL=http://localhost:3000/api/v1
