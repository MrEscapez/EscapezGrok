# Extra aliases (naast plugin.yml) — mapping alias -> target command name
# Wordt bij enable/reload gesynchroniseerd naar de command map waar mogelijk.
# Houd in sync met commands.yml aliases + plugin.yml aliases.
dc: discord
web: website
site: website
stem: vote
store: shop
rules: regels
team: staff
