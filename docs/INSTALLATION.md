# Installation

1. Build EscapezCore with Java 21: `cd minecraft/escapezcore && ./gradlew build`
2. Copy `build/libs/EscapezCore-0.1.0.jar` to the Paper `plugins/` folder.
3. Start the server once to generate configs.
4. Keep `database.enabled: false` unless PostgreSQL is ready.
